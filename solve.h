#ifndef SOLVE_H
#define SOLVE_H
#define scalar double
scalar solve(const int N, scalar tol, int max_iter, const scalar* p, const scalar *m, scalar* x, scalar* stats, const int stats_size) ;
#endif  // SOLVE_H
